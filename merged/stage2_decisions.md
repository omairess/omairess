# Stage 2 — integration decisions (one-line audit notes)

Each non-obvious choice, for review. File references point at the `DECISION:` comments in code.

## Architecture

1. **Three Shiny modules, not renamed IDs** — `NS()` namespacing structurally kills the `run`/`nodeBorderColor`/`layout` collisions and lets each tab keep its own layout vocabulary (semantic conflict dissolved, not papered over). (`app.R`)
2. **One app-wide recorder instance** — rules 6+8 demand a single exported script/log describing everything the app did; per-tab recorders would fragment it. (`01_recorder.R`)
3. **Script fragments ordered phase-first, insertion-second** — guarantees the export runs top-to-bottom in a clean session even when the user hops between tabs out of order. (`01_recorder.R`, unit-tested)
4. **`set.seed(<seed>)` re-emitted before every stochastic fragment**, not once at the top — reproduces the app's numbers regardless of which stages a colleague re-runs. (`01_recorder.R`)
5. **Tab fragments bake their variable subset as an explicit `c("V1",...)` literal** — editing the shared selection after running tab A can't silently redefine tab A's exported code. (`01_recorder.R`, `04_varselect_module.R`)
6. **New data upload drops every downstream recorder fragment (all tabs)** — an exported script must never mix fragments computed from different datasets. (`03_data_module.R`)
7. **Canonical data format is wide with underscore `var_<time>` naming** — Dagger's temporal auto-detection greps `_t\d+$`/`_\d+$` and would silently fail on base `reshape()`'s dot naming; both raw and pivoted frames get previews (fixes PsychoNetrix's invisible pivot). (`03_data_module.R`)
8. **`pickerInput` (PsychoNetrix's pattern) is the one variable-selection paradigm** — the only rule-3-compliant one of the three; dual-listbox and drag-and-drop dropped. (`04_varselect_module.R`)
9. **Pastel1-derived defaults replace all three source palettes** — rainbow(), sequential "Blues", and hardcoded hex arrays all dropped per rule 9. (`02_palette.R`)
10. **Dagger's unsigned arcs map onto the positive-edge picker only** — bnlearn arcs have no sign; we don't fake one. (`02_palette.R`, `05_appearance_module.R`)
11. **Figure export re-plots a stored zero-arg closure into a fresh device** — gives BootSON export it never had, adds SVG everywhere. (`05_appearance_module.R`)
12. **Package guard installs missing-only, never updates** — with the version ledger recorded as pipeline step 1 and in the script header (rule 0). (`00_packages.R`)

## Correctness wiring (network-psychometrics skill)

13. **Bootnet bootstraps are not opt-in** — "Estimate & validate" runs estimation → nonparametric boot → case-dropping boot as one recorded pipeline; centrality is locked until they complete. (`10_tab_bootnet.R`)
14. **CS gate is per-statistic and hard** — CS < 0.25 disables the ordered centrality plot; values remain visible only alphabetically under a red banner (transparency without ordering claims); 0.25–0.5 shows a caution band. (`10_tab_bootnet.R`)
15. **Gate keys on the leading interpreted measure** — expected influence when negative edges exist, strength otherwise. (`10_tab_bootnet.R`)
16. **EI force-included and listed first whenever any edge is negative** — strength misleads when opposite-sign edges cancel; both computed transparently as `colSums` on the weights matrix, not via helpers whose defaults may drift. (`10_tab_bootnet.R`)
17. **`corMethod` default flipped to `cor_auto`** — fixes BootSON's Pearson-by-default bug for Likert items; estimator/corMethod/γ pinned in every call and every fragment. (`10_tab_bootnet.R`)
18. **DAG: the bootstrap-averaged network is the reported model** — the single algorithm run is demoted to "exploratory fit", commented out in the export. (`11_tab_dagger.R`)
19. **Equivalence-class audit runs before any `cextend()`** — cextend manufactures directions; a caveat computed after it would understate what the data can't identify. (`11_tab_dagger.R`)
20. **CPDAG caveat is a named-arcs UI banner, not boilerplate** — lists exactly which arcs have unidentified direction, plus bootstrap direction-confidence (≈0.5 = undecidable) in the arc table. Dagger's "gold standard for causal discovery" copy is not carried over. (`11_tab_dagger.R`)
21. **psychonetrics estimator resolved before fitting** — the string "default" never reaches `runmodel`, the log, or the export; "auto" resolves to FIML when NAs present (skill prefers FIML), ML otherwise, and the resolved value is what's recorded. (`12_tab_psynet.R`)
22. **Ordinal+ML guard added** — warns when ordered/dichotomous data meet an ML-family estimator (the failure mode PsychoNetrix missed). (`12_tab_psynet.R`)
23. **NCT design (paired vs independent) is an explicit required radio** — `paired=` is always passed; paired path validates subject alignment (ID-matching UI is a stage-3 stub with a hard equal-n guard until then). (`12_tab_psynet.R`)
24. **NCT edge p-values corrected (Holm default)** — passed natively via `p.adjust.methods` when the installed NCT supports it, else applied manually to `einv.pvals`; significance flags computed on corrected values only. (`12_tab_psynet.R`)
25. **NCT estimation pinned to the bootnet tab's own settings reactive** — one source of truth for default/corMethod/γ, so the comparison can never silently use different settings than the networks on screen; the log also states "a null NCT is not proof of equality". (`12_tab_psynet.R`, `app.R`)

## Stage-3 boundaries

All mechanical work is marked `TODO(stage3-...)` in code: file-loader dispatch + loader UI, pickerInput/colourpicker/slider wiring, per-figure download buttons, Bridge-Symptoms + NSON port, Dagger constraint builder/split/temporal-graph port, psychonetrics non-GGM families + lambda editor port, NCT subject-ID matching UI. **Stage 3 must not alter anything above the TODO lines in the three tab files or in the recorder.**

## Post-Stage-3 user change requests (2026-07-05)

26. **Bootnet bootstraps made on-demand** (user override of decision #13) — estimation is now instant; a separate "Run bootstrap validation" button runs the nonparametric + case-dropping bootstraps. **The correctness gate is unchanged**: centrality and edge-CI panels stay locked (`validate()` refusal) until validation has run for the *current* network, and re-estimating drops the stale `bootnet_stability` fragment and nulls `rv$cs` so the gate re-locks. Only the *timing* moved, not the requirement. (`10_tab_bootnet.R`)
27. **Node size can scale by column mean** (rule-5 extension) — appearance module gains a "Scale node size by column mean" toggle + min/max range slider; both network tabs map lowest mean → smallest node, highest → largest, via a shared `scale_vsize_by_mean()` whose exact definition is emitted into the exported script. (`05_appearance_module.R`, `10_tab_bootnet.R`, `11_tab_dagger.R`)
28. **All BootSON estimators restored to the bootnet tab** — EBICglasso, ggmModSelect, pcor, cor, huge, **IsingFit** (binary, with a validity check), **mgm** (mixed), and **relimp** (relative-importance, plotted directed & non-negative). Each pins its own settings explicitly and records its own fragment. (NB: this is the *observed-variable* relative-importance network; PsychoNetrix's separate *latent* RI network (Johnson/LMG) remains a psychonetrics-tab port.) (`10_tab_bootnet.R`)
29. **DAG blacklist/whitelist constraint builder** — FROM×TO cross-product accumulation into blacklist/whitelist tables, threaded into `algorithm.args` so they apply to **every** bootstrap replicate (not just a single fit); a whitelist that forces a cycle is caught and surfaced. Constraints are baked as literal `data.frame`s into the exported script. (`11_tab_dagger.R`)
30. **DAG advanced arc metrics (direction × strength)** — arc width/labels can show boot.strength's `strength` (P present), `direction` (P orientation | present), or their `combined` product; the arc table always lists all three so nothing is hidden. (`11_tab_dagger.R`)
31. **Layout options** — bootnet tab: spring / circle / EGA-communities / PCA; DAG tab: spring / circle / hierarchical-Sugiyama. EGA is used for community colouring/layout only — the reported network is always the pinned estimator result. (`10_tab_bootnet.R`, `11_tab_dagger.R`)

## Psychonetrics family port (2026-07-05, continued)

32. **CFA/LNM/RNM/LRNM ported behaviour-faithfully** — one builder switch (rnm/lrnm get `omega_epsilon = "full"`), then runmodel → optional prune(alpha, adjust) → stepup → modelsearch in the original's order; ordered/dichotomous data passed via `ordered =` so psychonetrics uses polychoric input; optimizer pinned to nlminb and recorded. The exported fragment bakes the lambda matrix as a literal `matrix()` call. (`12_tab_psynet.R`, from `PsychoNetrix.R:1315-1409`)
33. **Lambda editor ported** — latent-names input → checkbox grid → `get_lambda()`, plus auto-simple-structure; fitting validates that lambda rows still match the current variable selection (guards against editing the selection after generating the editor). (`12_tab_psynet.R`)
34. **Latent RI network is the ORIGINAL algorithm, verbatim** — `johnson_rw_from_cor` (exact Shapley/LMG decomposition) copied unchanged from `PsychoNetrix.R:2135-2175`; verified bit-identical against the original on test data, and verified against the analytic invariant colSums(RI)[i] = R² of outcome i on the other latents. The exported script embeds the function via `deparse()` of the very object the app runs — zero-drift by construction. Plotted DIRECTED with the j→i = "j's share of i's R²" reading stated in the UI. (`12_tab_psynet.R`)
35. **NCT paired designs now align by subject ID** — an ID-column select appears for paired designs; rows are matched subject-by-subject (deduped, unmatched dropped and reported, minimum 10 pairs enforced); the no-ID fallback keeps the equal-n guard and states the row-order assumption out loud in the log. (`12_tab_psynet.R`)

## Second batch of user change requests (2026-07-05)

36. **DAG default edge scaling = 2** — `appearanceUI(..., default_esize = 2)` on the DAG tab. (#1)
37. **Equal-size node labels on every network** — all qgraph calls use `label.scale = FALSE`, so labels render at one fixed cex rather than being shrunk to fit each node. Centralised in `house_qgraph_args()`. (#2)
38. **Edge labels: show + size, everywhere** — the appearance module gains a "Show edge values as labels" toggle and an "Edge label size" slider (`edge.label.cex`); wired through all three tabs. (#3)
39. **RI network shows labels by default** — node labels are always explicit names (never numbered-with-legend) via `labels = colnames(W)`; the psychonetrics appearance panel defaults edge labels ON so the RI weights show on the arrows. (#4)
40. **Psychonetrics results expanded** — a results tabset with (a) fit indices *interpreted* (Excellent/Good/Acceptable/Poor + references, ported from `PsychoNetrix.R:1535-1707`), (b) a parameters table, (c) modification indices (largest first), (d) a matrices viewer. Lambda editor (factor names → assign variables) and loading-vs-variance identification were already present. (#5)
41. **NCT moved to its own top-level tab** — "Compare networks (NCT)", no longer nested in the psychonetrics tab; still consumes the GGM tab's settings reactive. (#6)
42. **EGA/PCA colour nodes by community** — EGA (walktrap) communities drive node colour for the EGA and PCA layouts, and via a standalone "Colour nodes by EGA community" toggle for any layout. (#7)
43. **Bootnet: explicit bootstrap-type choice** — checkboxes for nonparametric and/or case-dropping; both on by default. The CS gate is unchanged: skipping the case-dropping bootstrap leaves centrality locked. (#8)
44. **Minimum edge value to display, everywhere** — appearance-module numeric input mapped to qgraph `minimum`. (#9)
45. **Group comparison shows networks side by side** — the NCT tab renders the two group networks on a shared averaged layout (so node positions match) with each group named in its panel title. (#10)
46. **Shared qgraph contract** — `house_qgraph_args()` + `house_qgraph_args_code()` centralise the label/edge-label/minimum/colour rules so all three tabs stay consistent and the exported script matches the on-screen plot. (`05_appearance_module.R`)

## Third batch of user change requests (2026-07-05)

47. **Community colouring bug fixed** — qgraph's `groups` argument requires node *indices*; passing node *names* coloured the legend but left every node white (the reported symptom). Now `split(seq_len(ncol(W)), ega$wc)` with "Community k" names. (#1, `10_tab_bootnet.R`)
48. **Resizable plot windows** — a "Plot window height (px)" slider in the appearance module; each tab's network plot is wrapped in a `renderUI` whose height follows it. (#2/#6, all tabs)
49. **Case-dropping stability plot shown** — `plot(boot_cd)` (the average-correlation-vs-sampled-cases curve) now renders under the edge-CI plot once the case-dropping bootstrap has run. (#3, `10_tab_bootnet.R`)
50. **DAG direction threshold** — the display shows every bootstrap arc with strength >= threshold-1 AND direction >= threshold-2, applied live; both at 0 shows every arc that ever appeared (DAGger behaviour). The *reported* model (recorder/CPDAG audit) remains `averaged.network` at the strength threshold, and the plot fragment says which set it draws. (#4, `11_tab_dagger.R`)
51. **Tabs renamed** — "PMRFs (bootnet)", "DAGs (bnlearn)", "LNMs (psychonetrics)". (#5, `app.R`)
52. **Factor-structure plots ported** — "Factor structure + latent network" and "+ RI latent network" composite views (from `PsychoNetrix.R:2439-2588`): latents as circles on an inner ring (one pastel per factor), indicators as squares fanned around their dominant factor, directed loading arrows, small cross-loadings display-boosted (labels show true values), full-name labels everywhere; simple matrix plots also restore psychonetrics' empty dimnames so nodes are never numbered. (#6, `12_tab_psynet.R`)
53. **Multiple blacklist/whitelist batches** — each "Add" click appends the FROM×TO cross-product; rows are click-selectable with per-list "Remove selected" and "Clear" buttons. (#7, `11_tab_dagger.R`)
54. **Clean cascade layout** — DAGger's layered cascade ported: Katz-centrality bands (snap to 8 levels, even x-spread within a band, no overlap) plus the per-edge curve matrix that bends any arc that would pass through an intermediate node. Verified with unit tests (layering, same-band spread, curve-through-node detection). Rgraphviz was considered and rejected — it is a Bioconductor dependency the install-missing-only package guard cannot fetch from CRAN. (#8, `11_tab_dagger.R`)

## Fourth batch of user change requests (2026-07-06)

55. **RIN numbered nodes fixed** — `estimateNetwork(default = "relimp")` returns a weights matrix with EMPTY dimnames, so qgraph numbered the nodes; variable names are now restored from the analysis data before plotting, and `house_qgraph_args()` gained a final name fallback. (#1, `10_tab_bootnet.R`, `05_appearance_module.R`)
56. **SEM path diagram** — "Path diagram (SEM, semPlot)" plot type ported from `PsychoNetrix.R:3078-3117`: `semPlot::lisrelModel(LY, PS, TE)` from lambda / sigma_zeta / sigma_epsilon with dimnames restored, `semPaths(what = "std", layout = "tree2")`. semPlot added to APP_PACKAGES. (#2, `12_tab_psynet.R`)
57. **Latent node size scales by factor score, not column means** — `mean_fscores_psynet()` ported from `PsychoNetrix.R:2189-2212` (regression-method scores, uncentered = severity level); |mean factor score| drives latent node size in the latent, RI, and composite plots when node scaling is on. Observed nodes keep column-mean scaling. (#3, `12_tab_psynet.R`)
58. **Predictability rings everywhere relevant** — appearance module gains show/colour/thickness controls; observed networks (PMRF tab, omega/residual) use OLS R² (`node_predictability_r2`, `PsychoNetrix.R:1971-1986`), latent networks use the analytic R² = 1 − 1/K_ii from sigma_zeta (`latent_predictability_r2`, `:1991-2004`), composite factor plots ring the latents only (NULL slots on indicator squares, the original's `pie_args` list convention). Verified against direct regression R². DAG arcs excluded — predictability is undefined for bnlearn structures. (#4, `05_appearance_module.R`, all tabs)
59. **Per-module data transforms** — each analysis tab gets a "Data transformation" select (none / mean-center / z-score / nonparanormal) applied after variable selection and before the analysis; the npn is the rank-based normal-scores transform ported from `PsychoNetrix.R:930-937` (NA-safe). Verified that the exported script fragments reproduce the app's transformed data bit-exactly. (#5, `03_data_module.R` + all three tabs)
60. **Organic DAG layout controls** — honest graphviz translation (Rgraphviz is Bioconductor-only, unreachable for the CRAN-only installer): "Layered / dot-like" = Sugiyama with **ranksep** (vgap) and **nodesep** (hgap) sliders, an **organic blend** slider that mixes the layered coordinates with a stress layout to break exact vertical rank alignment, and a **use-arc-strength-as-layout-weights** toggle (the graphviz `weight` analogue; KK gets inverse weights since it treats weights as target distances); "Stress majorization (neato-like)" = Kamada-Kawai; "Force-directed (fdp-like)" = seeded Fruchterman-Reingold; **Manual mode** = editable node-coordinate table pinned across replots (pos/pin analogue), initialised from the last computed layout, with pinned coordinates baked as a literal into the exported script. Arrows, edge widths, and edge labels are computed independently of layout and persist across all engines. (#6, `11_tab_dagger.R`)
